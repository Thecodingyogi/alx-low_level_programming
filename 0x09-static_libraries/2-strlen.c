#include "main.h"
#include <stdio.h>

/**
 * _strlen - prints length of a string
 * @s: string to be printed
 *
 * Return: length of string
 */

int _strlen(char *s)
{
	int len;

	for (len = 0; s[len] != '\0'; len++)

	return (len);
}
